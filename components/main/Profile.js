import React from 'react'
import { View, Text } from 'react-native'

export default function Profile(){
    return(
        <View style={{justifyContent: 'center', marginTop: 40, marginHorizontal: 20}}>
            <Text style={{fontSize:24}}>Profile</Text>
        </View>
    )
}